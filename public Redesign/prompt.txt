I need two seperate pages first off is a simple login/create account page.
feature name, email, and password for the create page and email and password for login
also include the name TodoLander.
next I need a dashboard page. this page features the title, a bar including:
a search bar, settings, sign out, import (JSON), export (JSON, iCal), and a little info button to show JSON formatting
Heres the JSON formatting: 
The import file must be a .json file containing an object where each key is a date in YYYY-MM-DD format and each value is an array of tasks for that day.

Tasks can be plain strings or objects with a text field. The color field is optional:

{
  "2026-03-10": [
    "Team standup at 9am",
    "Finish project proposal"
  ],
  "2026-03-11": [
    { "text": "Doctor appointment at 2pm" },
    { "text": "Renew car registration", "done": true },
    { "text": "Buy groceries", "color": "#22c55e" }
  ]
}
Supported colors: #ef4444 red, #f97316 orange, #eab308 yellow, #22c55e green, #3b82f6 blue, #6c63ff purple, #ec4899 pink. Duplicate tasks (same text on the same day) are skipped automatically.

next In the dashboard I need a calander with each day a button to go to today, a filter option based of the colors specified above,
a delete all button, the ability to click on each day and scroll through months. 
Next in the dashboard I need the actual todo section, including the list itself, a way to add an item to the todo list, with a selector for repeating tasks
In this section I also need a clear done button, and a bar to share what percentage of todays tasks are done.

I want a neumorphism style. 